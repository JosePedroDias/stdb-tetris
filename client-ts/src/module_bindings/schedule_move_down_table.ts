// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  CallReducerFlags,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  DbContext,
  ErrorContextInterface,
  Event,
  EventContextInterface,
  Identity,
  ProductType,
  ProductTypeElement,
  ReducerEventContextInterface,
  SubscriptionBuilderImpl,
  SubscriptionEventContextInterface,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
} from "@clockworklabs/spacetimedb-sdk";
import { ScheduleMoveDown } from "./schedule_move_down_type";
import { EventContext, Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `schedule_move_down`.
 *
 * Obtain a handle from the [`scheduleMoveDown`] property on [`RemoteTables`],
 * like `ctx.db.scheduleMoveDown`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.scheduleMoveDown.on_insert(...)`.
 */
export class ScheduleMoveDownTableHandle {
  tableCache: TableCache<ScheduleMoveDown>;

  constructor(tableCache: TableCache<ScheduleMoveDown>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<ScheduleMoveDown> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `id` unique index on the table `schedule_move_down`,
   * which allows point queries on the field of the same name
   * via the [`ScheduleMoveDownIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.scheduleMoveDown.id().find(...)`.
   *
   * Get a handle on the `id` unique index on the table `schedule_move_down`.
   */
  id = {
    // Find the subscribed row whose `id` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: bigint): ScheduleMoveDown | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.id, col_val)) {
          return row;
        }
      }
    },
  };
  /**
   * Access to the `board_id` unique index on the table `schedule_move_down`,
   * which allows point queries on the field of the same name
   * via the [`ScheduleMoveDownBoardIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.scheduleMoveDown.board_id().find(...)`.
   *
   * Get a handle on the `board_id` unique index on the table `schedule_move_down`.
   */
  board_id = {
    // Find the subscribed row whose `board_id` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: number): ScheduleMoveDown | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.board_id, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: ScheduleMoveDown) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: ScheduleMoveDown) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: ScheduleMoveDown) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: ScheduleMoveDown) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: ScheduleMoveDown, newRow: ScheduleMoveDown) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: ScheduleMoveDown, newRow: ScheduleMoveDown) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
